spring:
  profiles:
    active: "local"
  datasource:
    url: jdbc:mysql://localhost:3306/chek?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 3609
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: create
      properties:
        show_sql: true
        format_sql: true
      database: mysql
      generate-ddl: true
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
